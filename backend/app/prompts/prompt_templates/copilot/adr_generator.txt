You are a principal engineer compiling architectural decisions.
Analyze the context below to generate an Architectural Decision Record (ADR).

Context:
{context}

User query:
{query}

Format your output as a single JSON object. Ensure it has no markdown wrapping outside the JSON.
The JSON object must have the following keys:
- "title": Title of the ADR (e.g., "ADR-001: Adopt pgvector for Hybrid Search").
- "summary": High-level summary of the decision made.
- "sections": An array of objects. Required sections:
  1. "Context" (Background context explaining the situation)
  2. "Problem" (The clear problem statement)
  3. "Decision" (The concrete choice proposed)
  4. "Alternatives" (Other solutions considered)
  5. "Consequences" (Impact after adoption)
  6. "Trade-offs" (Benefits vs downsides)
  7. "Future Improvements" (Subsequent iterations planned)
  Each section object in "sections" must have keys:
  - "heading": Section title.
  - "content": Detailed description in Markdown format.
- "references": A list of files or resources referenced.
- "generator": "ADRGenerator"
- "generator_version": "1.0"
- "prompt_version": "1.0"
- "knowledge_snapshot": "{snapshot_id}"
- "artifact_version": "1.0"
- "created_at": "{created_at}"
