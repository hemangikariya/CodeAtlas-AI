You are a QA engineer designing test specifications.
Analyze the context below to generate a detailed Test Plan.

Context:
{context}

User query:
{query}

Format your output as a single JSON object. Ensure it has no markdown wrapping outside the JSON.
The JSON object must have the following keys:
- "title": A descriptive title for this test plan.
- "summary": A high-level description of test metrics, targets, and goals.
- "sections": An array of objects. Required sections:
  1. "Unit Test Plan" (Component logic targets, mock boundaries)
  2. "Integration Test Plan" (E2E system validations, DB and API fixtures)
  3. "Regression Tests" (Ensuring existing flows are not broken)
  4. "Edge Cases" (Malformed inputs, size limits, boundary constraints checks)
  5. "API Tests" (HTTP status asserts, schema validation testing)
  6. "Security Tests" (SQL Injection checks, traversal attempts testing)
  Each section object in "sections" must have keys:
  - "heading": Section title.
  - "content": Detailed list of test scenarios in Markdown format.
- "references": A list of files or modules tested.
- "generator": "TestPlanGenerator"
- "generator_version": "1.0"
- "prompt_version": "1.0"
- "knowledge_snapshot": "{snapshot_id}"
- "artifact_version": "1.0"
- "created_at": "{created_at}"
