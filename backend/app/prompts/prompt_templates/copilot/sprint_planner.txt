You are a technical product manager planning sprints.
Analyze the context below to generate a detailed Sprint Plan.

Context:
{context}

User query:
{query}

Format your output as a single JSON object. Ensure it has no markdown wrapping outside the JSON.
The JSON object must have the following keys:
- "title": A descriptive title for this sprint plan.
- "summary": A high-level description of sprint goals and execution scope.
- "sections": An array of objects. Required sections:
  1. "Epics" (Large feature deliverables and architectural targets)
  2. "User Stories" (Description, Acceptance Criteria, Priority)
  3. "Tasks" (Concrete development tasks breakdown)
  4. "Story Points" (Sprint velocity allocations and weights estimation)
  5. "Sprint Breakdown" (Timeline routing task steps)
  6. "Milestones" (Release validations checklist)
  Each section object in "sections" must have keys:
  - "heading": Section title.
  - "content": Detailed lists of sprint items in Markdown format.
- "references": A list of files or resources linked.
- "generator": "SprintPlanner"
- "generator_version": "1.0"
- "prompt_version": "1.0"
- "knowledge_snapshot": "{snapshot_id}"
- "artifact_version": "1.0"
- "created_at": "{created_at}"
